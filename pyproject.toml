[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
# Correctness only (pyflakes): unused imports/variables, undefined names,
# shadowed globals. Style rules are a separate, deliberate decision.
select = ["F"]
