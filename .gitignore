__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# feed-quality-audit run outputs (persisted as workflow artifacts, not git)
state.json
prev-state.json
report.md

# nightly audit: redacted artifact copy
public-state.json
